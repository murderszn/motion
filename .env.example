# MongoDB Atlas connection URI (required for the workshop signup API)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/motion?retryWrites=true&w=majority

# Port for the Express API server (optional, defaults to 3001)
API_PORT=3001
